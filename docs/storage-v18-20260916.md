# V18 storage enumeration

V17 established working high-speed USB serial logging and returned successfully to stock Android. V18 retains its exact kernel, RAM filesystem, boot arguments, USB power setting, voltage constraints and overlay. Its sole parsed device-tree change enables `/soc@0/mmc@c0c4000`.

The eMMC setup retains an eight-bit bus, non-removable card, 50 MHz maximum and stock-derived supply assignments: pm660l_l4 for VMMC and pm660_l8 for VQMMC. HS200/HS400 capabilities are not enabled. The diagnostic init neither opens block devices nor mounts persistent filesystems. Kernel card enumeration and partition scanning are the intended observations; successful enumeration alone will not establish sustained storage integrity or Android compatibility.

Candidate SHA-256: `a983ef25bdca4a06dae88ab74a4db8582b0a4507f01e14ad6bb2a9354c123ee6`.

Packaging, captured-bootloader checks, six recovery protocol checks, four transition checks and remote staged-payload verification passed. Installation accepts only the exact stock or V17 recovery and exact known boot messages, then verifies full readbacks. Normal Android remains the fallback.

USB stages remain configure at 0 seconds, bind at 4, connect at 8 and serial at 12 after RAM init. The host capture waits for a heartbeat at 30 seconds rather than 14 to include deferred storage-driver retries, bounded by 45 seconds after fastboot departure. No camera is needed if USB still works.

If storage is absent, inspect probe/deferred messages, regulator loads and pin/clock dependencies before changing more settings. Local sdhci-msm code calls regulator_set_load for VMMC/VQMMC; current L4b/L8a constraints do not yet include regulator-allow-set-load. Those properties were deliberately left unchanged for this first isolation test. The stock A6L's eMMC consumer specifies VMMC 2.95 V, loads 200/570000 uA, and VQMMC 1.8 V, loads 200/325000 uA. Do not infer that enabling the controller proves those dependencies are sufficient.

Installation completed at 12:39:51 UTC. All twelve copied readbacks passed independent desktop verification; exact V17 predecessor and known boot message preserved. Host services restored. Phone powered off; requested normal Android startup before the first diagnostic capture. V18 capture has not started.
