# eink3: the patched drm_hwcomposer serves DRM leases of the ignored e-ink connector on an abstract socket
allow hal_graphics_composer_default self:unix_stream_socket { create bind listen accept read write getopt setopt };
